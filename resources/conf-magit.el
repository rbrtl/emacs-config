;; magit related configs

(setq magit-last-seen-setup-instructions "1.4.0")

(global-set-key (kbd "C-c s") 'magit-status)
(global-set-key (kbd "C-c l") 'magit-log)
(global-set-key (kbd "C-c d") 'magit-ediff)
